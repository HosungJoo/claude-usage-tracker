/**
 * Claw'd 팔레트.
 *
 * 몸은 원본 그대로 코랄 단색이다 — 외곽선도 음영도 없다. 색이 늘어나는
 * 곳은 몸이 아니라 **표정과 액세서리**뿐이다. 몸에 색을 더하고 싶어지면,
 * 원본에 없다는 이유만으로 충분하다.
 */
export const PALETTE = {
  /** 원본에서 그대로 뽑은 값. */
  body: '#d97757',

  /**
   * 눈.
   *
   * 원본에서 눈은 배경이 비치는 구멍이다. 하지만 이 캐릭터는 어떤 바탕
   * 위에 뜰지 알 수 없으므로 어두운 색으로 메운다.
   */
  eye: '#241812',

  /** 놀랐을 때 크게 뜬 눈의 흰자. */
  eyeWhite: '#fff4ec',

  /** 식은땀. 한도가 가까워질수록 늘어난다. */
  sweat: '#6fb6e4',

  /** 머리 위 느낌표. 주의를 끄는 유일한 노란색이다. */
  alert: '#ffcc4d',

  /** 기절했을 때 머리 위를 도는 표시. */
  dizzy: '#b9927f',
} as const;

export type PaletteKey = keyof typeof PALETTE;
