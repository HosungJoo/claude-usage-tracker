/**
 * Claw'd 팔레트.
 *
 * 원본 아트워크는 코랄 단색 하나로만 그려져 있다 — 외곽선도, 음영도,
 * 하이라이트도 없다. 그 절제가 캐릭터의 인상을 만들므로 그대로 지킨다.
 * 색을 더 넣고 싶은 유혹이 생기면, 원본에 없다는 이유만으로 충분하다.
 */
export const PALETTE = {
  /** 원본에서 그대로 뽑은 값. */
  body: '#d97757',

  /**
   * 눈.
   *
   * 원본에서 눈은 배경이 비치는 구멍이다. 하지만 이 앱의 캐릭터는 어떤
   * 바탕 위에 뜰지 알 수 없으므로, 밝은 배경에서도 눈이 보이도록
   * 어두운 색으로 메운다. 원본과 다른 유일한 지점이다.
   */
  eye: '#241812',
} as const;

export type PaletteKey = keyof typeof PALETTE;
